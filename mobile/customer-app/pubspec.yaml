name: urbanshopper_customer
description: Urban Shopper - Customer App
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.0.0
  http: ^1.2.0
  provider: ^6.1.0
  google_maps_flutter: ^2.6.0
  geolocator: ^11.0.0
  image_picker: ^1.0.7
  intl: ^0.20.2
  web_socket_channel: ^3.0.0
  shared_preferences: ^2.2.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
